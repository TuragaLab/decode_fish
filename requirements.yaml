name: decode_fish_dev2

channels:
  - pytorch
  - defaults
  
dependencies:

  - python=3.7
  - pip
  - pytorch=1.7.1
  - cudatoolkit=10.1
  - torchvision
  - h5py
  - matplotlib
  - pandas
  - pyyaml
  - scipy
  - scikit-image
  - seaborn=0.10
  - tifffile>=2020.2
  - tqdm
#   - hydra-core
  - pip:
      - nbdev
      - monai
      - wandb
      - hydra-core
      - numba
      - sklearn

  # add kernel to jupyter
  - ipykernel
  
 # conda install tensorflow-gpu=2.4.1=h30adc30_0